import 'package:flutter/material.dart';

class PayrollMainPage extends StatelessWidget {
  const PayrollMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Payroll')),
    );
  }
}
